<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/nav.ejs') %>
    <div class="player content">
      <h2>Player:</h2>
      <% if (aPlayer.length > 0) var player = aPlayer[0];{ %>
      <h3>
        <%= player.first_name %> <%= player.last_name %> <%= player.team_name %>
      </h3>
      <% } %>
    </div>
  </body>
  <%- include('./partials/foot.ejs') %>
</html>
